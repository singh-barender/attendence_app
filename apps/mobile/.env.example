# Copy to .env (gitignored) and adjust for your machine.
# Expo inlines EXPO_PUBLIC_* vars into the client bundle at build time —
# see src/services/graphqlClient.ts.
EXPO_PUBLIC_API_URL=http://localhost:4000
