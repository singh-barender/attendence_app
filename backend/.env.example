DATABASE_URL="file:./dev.db"

# Generate your own with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="replace-with-a-real-32-byte-hex-secret"

PORT=4000
NODE_ENV=development

# "Late" attendance status derivation (ADR-016) — 24h clock, single global value.
SHIFT_START_HOUR=9
