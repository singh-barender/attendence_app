# @attendance-app/shared-types

This package contains TypeScript types and schemas that are shared across the entire workspace (both frontend and backend).

It ensures that database models, GraphQL payloads, and domain objects remain tightly coupled and type-safe across the network boundary.
